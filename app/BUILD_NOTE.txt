# Build note

This project has been prepared and finalized for local build. The codebase includes placeholder usage for the Gemini API endpoint (GeminiClient); replace the endpoint and configure your GEMINI_API_KEY in local.properties.

Local build command:

./gradlew clean assembleDebug

Expected APK path after success:

app/build/outputs/apk/debug/app-debug.apk
